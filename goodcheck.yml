rules:
  - id: sider.devon_rex.ruby_version
    pattern: /ruby:\d+\.\d+\.\d+/
    message: Update also `.ruby-version` when updating a Ruby version.
    glob: "**/Dockerfile"

import:
  - https://raw.githubusercontent.com/sider/goodcheck-rules/master/rules/typo.yml
